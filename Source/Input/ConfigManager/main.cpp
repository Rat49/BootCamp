#include"ConfigManager.h"


int main() {
	/*
	std::string line = "ffgaa [dhswrra]";
	std::string value;
	std::regex pattern("\[[A-Za-z]*\]");

	if (std::regex_search(line, value, pattern)) {
		std::string categoryName = value.substr(1, value.size() - 2);

		std::cout << categoryName << std::endl;
	}*/
}